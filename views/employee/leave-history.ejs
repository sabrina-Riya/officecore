<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Leave History</title>
  <style>
    body { font-family: Arial; margin: 30px; background-color: #f9f9f9; }
    h2, h3 { color: #333; }
    table { width: 100%; border-collapse: collapse; margin-top: 20px; }
    th, td { padding: 10px; border: 1px solid #ccc; text-align: left; }
    th { background-color: #eee; }
    a { text-decoration: none; color: #007bff; }
    .success { color: green; }
    .error { color: red; }
  </style>
</head>
<body>
  <h2>Leave History</h2>
  <h3>Leave: <%= leave.reason %> (Status: <%= leave.status %>)</h3>
  <p>From: <%= new Date(leave.start_date).toLocaleDateString() %> To: <%= new Date(leave.end_date).toLocaleDateString() %></p>
  
  <h3>Audit Log</h3>
  <% if (logs.length === 0) { %>
    <p>No history found for this leave.</p>
  <% } else { %>
    <table>
      <thead>
        <tr>
          <th>Action</th>
          <th>Old Status</th>
          <th>New Status</th>
          <th>Message</th>
          <th>Date</th>
        </tr>
      </thead>
      <tbody>
        <% logs.forEach(log => { %>
          <tr>
            <td><%= log.action %></td>
            <td><%= log.old_status || '-' %></td>
            <td><%= log.new_status || '-' %></td>
            <td><%= log.message %></td>
            <td><%= new Date(log.created_at).toLocaleString() %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  <% } %>

  <br>
  <a href="/employee/leave-list">Back to Leave List</a>
</body>
</html>
