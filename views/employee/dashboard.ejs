<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Employee Dashboard</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f6f8;
      margin: 30px;
    }

    h2 {
      color: #333;
    }

    a {
      text-decoration: none;
      color: #007bff;
      margin-right: 15px;
    }

    a:hover {
      text-decoration: underline;
    }

    .cards {
      display: flex;
      gap: 20px;
      margin-top: 25px;
      flex-wrap: wrap;
    }

    .card {
      background: #fff;
      padding: 20px;
      width: 200px;
      text-align: center;
      border-radius: 8px;
      box-shadow: 0 3px 10px rgba(0,0,0,0.1);
    }

    .card h3 {
      margin: 0;
      color: #555;
    }

    .card span {
      display: inline-block;
      margin-top: 10px;
      background: #007bff;
      color: #fff;
      padding: 6px 12px;
      border-radius: 6px;
      font-weight: bold;
    }

    .success { color: green; }
    .error { color: red; }
  </style>
</head>
<body>

  <a href="/logout">Logout</a>

  <!-- ✅ FIXED VARIABLE -->
  <h2>Welcome <%= username %></h2>

  <% if (success_msg && success_msg.length > 0) { %>
    <p class="success"><%= success_msg %></p>
  <% } %>

  <% if (err_msg && err_msg.length > 0) { %>
    <p class="error"><%= err_msg %></p>
  <% } %>

  <div class="cards">
    <div class="card">
      <h3>Total Leaves</h3>
      <span><%= totalleave %></span>
    </div>

    <div class="card">
      <h3>Approved</h3>
      <span><%= approvedLeave %></span>
    </div>

    <div class="card">
      <h3>Rejected</h3>
      <span><%= rejectedLeave %></span>
    </div>
  </div>

  <br><br>

  <a href="/employee/leave-apply">Apply for Leave</a>
  <a href="/employee/leave-list">My Leave Requests</a>

  <footer style="margin-top:40px;color:#666;">
    © 2025 OfficeCore
  </footer>

</body>
</html>
